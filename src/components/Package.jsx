import { PackageCard } from './PackageCard.';

export const Package = () => {
    return (
        <div className="h-screen v-padding h-padding max-w-7xl mx-auto">
            
        </div>
    );
};
